# NotionAgent

An agent that reads and retrieves information from a user's Notion workspace.

``` shell
pip install -r requirements.txt
```


http://localhost:8000/docs#/

# NotionAgent FE
https://github.com/jonitox/NotionAgent-FE
